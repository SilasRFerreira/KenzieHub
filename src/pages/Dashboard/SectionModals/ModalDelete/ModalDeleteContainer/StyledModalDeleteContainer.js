import { styled } from 'styled-components';

const StyledModalDeleteContainer = styled.div`
    width: 369px;
    max-width: 90%;

    background-color: var(--color-gray-300);

    border-radius: var(--radius);
`

export default StyledModalDeleteContainer