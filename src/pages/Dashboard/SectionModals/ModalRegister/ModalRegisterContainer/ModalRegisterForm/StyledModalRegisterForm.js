import { styled } from "styled-components";

const StyledModalRegisterForm = styled.form`
    padding: 1rem;
    
    background-color: var(--color-gray-300);

    display: flex;
    flex-direction: column;

    gap: 1rem;
`

export default StyledModalRegisterForm