import { styled } from "styled-components";

const StyledSectionTechs = styled.section`
    margin: 1rem auto;
    
    .dashboard-container{
        display: flex;
        flex-direction: column;

        gap: 2rem
    }
`

export default StyledSectionTechs