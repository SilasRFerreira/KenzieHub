import { styled } from "styled-components";

const StyledTechsList = styled.ul`
    background-color: var(--color-gray-300);

    border-radius: var(--radius);

    padding: 1rem 1.5rem;

    display: flex;
    flex-direction: column;

    gap: 1rem;
`

export default StyledTechsList