import { styled } from 'styled-components';

const StyledHeaderLogin = styled.header`
    margin-top: 3rem;

    display: flex;

    justify-content: center;

    div{
        display: flex;

        justify-content: center;
    }
`

export default StyledHeaderLogin